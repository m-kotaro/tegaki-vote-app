{
  "name": "@tegaki/shared",
  "version": "0.1.0",
  "private": true,
  "description": "型定義・定数・API 契約のみを保持する共有パッケージ（実行ロジックを含まない）",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  }
}
